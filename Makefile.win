.PHONY: all regen clean distclean

export PATH := $(PWD)/mxe-static/usr/bin/:$(PATH)

all:
	-$(MAKE) -C build-win

regen:
	mkdir -p build-win
	cd build-win ; i686-w64-mingw32.static-cmake .. -DWIN32=true

clean:
	$(MAKE) -C build-win clean

distclean:
	-rm -rf build-win/
